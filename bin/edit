#!/bin/bash

# Launch Sublime or vi depending on whether we're on a local Mac or in an SSH session
if type mate &> /dev/null; then
  if [ "$TERM_PROGRAM" == "Apple_Terminal" ]; then
    exec subl -w $@
  fi
fi

exec vi $@
